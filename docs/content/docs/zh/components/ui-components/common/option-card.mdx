---
title: OptionCard
description: 选项卡片组件
---

import { PropsTable, EventsTable, SlotsTable, MethodsTable } from '@/components/docs/api-table';

# OptionCard

展示可点击的选项卡片列表，常用于 AI 回复中的快捷选项。

## vue中使用

```vue
<template>
  <OptionCard
    :cards="options"
    :send-message="handleSendMessage"
  />
</template>

<script setup>
const options = ['选项一', '选项二', '选项三']

const handleSendMessage = (text) => {
  console.log('发送消息:', text)
}
</script>
```

## html/react或其他框架中使用

```html
<div id="containerId"></div>
<script>
ADPChatComponent.OptionCard.mount('#containerId', {
  cards: ['选项一', '选项二', '选项三'],
  sendMessage: (text) => console.log('发送消息:', text)
})
</script>
```

## API

<PropsTable data={[
  { name: 'cards', type: 'string[]', required: true, description: '选项卡片列表' },
  { name: 'sendMessage', type: '(text: string) => void', description: '发送消息回调' },
]} />
